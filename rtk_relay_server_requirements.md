# RTK中継サーバ 要件定義・設計ドキュメント（Draft）

## 目的・背景

路面データ収集プロジェクトにおいて、車両側GNSS受信機のRTK測位精度を確保するため、RTKプロバイダー（docomo /
SoftBank / KDDI等、**未選定**）が提供するNTRIPサービスを利用する。

当初はNTRIP
Casterを自前構築する方針だったが、運用負荷を避けるためRTKプロバイダー利用方針に変更した（[rtk_ntrip_communication_summary.md](./rtk_ntrip_communication_summary.md)参照）。

その上で、車両とRTKプロバイダーの間に**中継サーバ**を置く。単純に車両がプロバイダーへ直接接続する構成にしない理由は以下の通り。

-   プロバイダーの認証情報（Mountpoint / ID /
    Password）を車両側に配布・保持させない（漏えい・車両紛失時のリスク低減）
-   プロバイダーが未選定であり、将来的な切替・複数プロバイダー併用に対して車両側の変更を不要にする
-   車両ごとの接続状況・利用量を一元的に把握できるようにする（監視・将来的な課金按分の土台）

------------------------------------------------------------------------

## 対象範囲（本チームのスコープ）

このチーム（バックエンド/インフラ）が担当するのは以下。

-   RTK中継サーバ（本ドキュメントの対象）
-   計測データ収集・保存・BI加工サービス（別途、現在POC中）

以下は対象外・別チーム。

-   車載ソフトウェア（NTRIP Client実装）：本ドキュメントで固まった車両側インターフェース要件を後日連携
-   マップエンジンによる可視化アプリ

------------------------------------------------------------------------

## 全体アーキテクチャ

``` mermaid
flowchart LR
    GNSS["GNSS Receiver"]
    VCLIENT["NTRIP Client<br/>車載PC"]
    RELAY["RTK中継サーバ<br/>(AWS / 本チーム担当)"]
    PROVIDER["RTK Provider<br/>(未選定)"]
    SENSOR["LiDAR / Camera / IMU等"]
    COLLECT["データ収集サービス<br/>(AWS / 本チーム担当・POC中)"]
    BI["BIツール<br/>加工処理"]
    MAPAPP["マップエンジン<br/>可視化アプリ (別チーム)"]

    GNSS -->|"NMEA-GGA"| VCLIENT
    VCLIENT -->|"GGA (車両認証済み)"| RELAY
    RELAY -->|"GGA (プロバイダー認証)"| PROVIDER
    PROVIDER -->|"RTCM"| RELAY
    RELAY -->|"RTCM"| VCLIENT
    VCLIENT -->|"RTCM"| GNSS

    GNSS -->|"RTK FIX位置"| SENSOR
    SENSOR -->|"位置付き計測データ"| COLLECT
    COLLECT --> BI
    BI -->|"加工済みデータ"| MAPAPP
```

中継サーバは**NTRIP（GGA/RTCM）の中継のみ**を担当し、計測データパイプライン（データ収集サービス）とは疎結合に分離する。両者を混在させると障害影響範囲が広がり、スケーリング要件も異なる（中継サーバは低遅延・常時接続重視、データ収集サービスはスループット・保存重視）ため。

------------------------------------------------------------------------

## RTK中継サーバの役割

1.  **車両認証・認可**：本チームが発行する車両固有の認証情報でNTRIP
    Clientからの接続を検証する
2.  **プロバイダー認証情報の集中管理**：RTKプロバイダーのMountpoint /
    ID / Passwordを車両側に一切渡さず、中継サーバ内でのみ保持する
3.  **NTRIPセッション中継**：GGA（車両→プロバイダー）とRTCM（プロバイダー→車両）を双方向・低遅延で中継する
4.  **プロバイダー抽象化**：プロバイダー未選定のため、Provider
    Adapterパターンで実装し、選定後の切替・将来のマルチプロバイダー対応を容易にする
5.  **接続監視・再接続制御**：車両側/プロバイダー側いずれの切断も検知し、再接続をハンドリングする
6.  **利用ログ記録**：車両ごとの接続時間・データ量を記録する（将来の課金按分・利用状況分析の土台）

------------------------------------------------------------------------

## 機能要件

  ID     要件
  ------ --------------------------------------------------------------
  F-1    車両（NTRIP Client）ごとに個別の認証情報で中継サーバへ接続できること
  F-2    RTKプロバイダーの認証情報は中継サーバ内（Secrets Manager等）にのみ保持し、車両側からは不可視であること
  F-3    車両からのGGAをプロバイダーへ、プロバイダーからのRTCMを車両へリアルタイムに中継すること
  F-4    プロバイダー設定（host/port/mountpoint/認証情報）を中継サーバ側の設定変更のみで切替可能なこと（車両側の変更不要）
  F-5    車両側またはプロバイダー側の接続断を検知し、ログに記録すること
  F-6    プロバイダー側の切断時、中継サーバが自動再接続を試みること
  F-7    車両ごとの接続時間・通信量を記録し、後から集計可能な形で保存すること
  F-8    中継サーバの死活状態・プロバイダーとの接続状態を監視し、異常時にアラート可能なこと

------------------------------------------------------------------------

## 非機能要件

  ID     要件                                                                         備考
  ------ ---------------------------------------------------------------------------- --------------------------------
  N-1    中継によるRTK測位への遅延影響を最小化すること                                具体的な目標値は要検討（走行速度・車線判定精度から逆算）
  N-2    走行中の車両が測位を継続できる可用性を確保すること（マルチAZ構成）           
  N-3    車両台数の増加に対して水平スケール可能なこと                                 台数見込みは要確認
  N-4    車両認証情報・プロバイダー認証情報を分離管理し、通信はTLSで保護すること      
  N-5    プロバイダー切替・追加が中継サーバ側の設定変更のみで完結すること             車両側改修を発生させないため

------------------------------------------------------------------------

## AWS構成案（ラフ）

NTRIPは長時間持続するTCPベースの独自プロトコル（HTTPのGETをベースにするが、レスポンスがストリーミングであり一般的なHTTPセマンティクスに従わない）であるため、ALBではなく**NLB（TCP）+
ECS/Fargate**を軸に検討する。

``` mermaid
flowchart TD
    VCLIENT["NTRIP Client (車両)"]
    NLB["NLB (TCP)"]
    ECS["ECS/Fargate<br/>中継サーバアプリ"]
    SECRETS["Secrets Manager<br/>プロバイダー認証情報"]
    DDB["DynamoDB / RDS<br/>車両認証情報・接続ログ"]
    CW["CloudWatch<br/>メトリクス・アラーム"]
    PROVIDER["RTK Provider"]

    VCLIENT -->|"TLS"| NLB
    NLB --> ECS
    ECS -->|"認証情報取得"| SECRETS
    ECS -->|"認証・ログ記録"| DDB
    ECS -->|"NTRIP"| PROVIDER
    ECS -->|"メトリクス"| CW
```

-   **NLB**：TCPパススルーで低遅延を確保。車両側TLS終端はNLB or
    アプリ内のどちらにするか要検討
-   **ECS/Fargate**：中継アプリ本体。Provider
    Adapterを実装し、設定（DynamoDB/Parameter
    Store等）でプロバイダーを切替可能にする
-   **Secrets Manager**：プロバイダー認証情報の一元管理
-   **DynamoDB/RDS**：車両認証情報・接続ログ（車両台数・アクセスパターン次第でどちらか選定）
-   **CloudWatch**：死活監視・プロバイダー接続断のアラート

※ 本構成はラフ案であり、車両台数・レイテンシ要件が固まり次第、詳細設計（Auto
Scaling方針、TLS終端方式、ログ基盤の具体化）を行う。

------------------------------------------------------------------------

## 車両側（NTRIP Client）とのインターフェース（別チームへ連携予定）

以下は今後車載ソフトウェアチームへ提示する項目。現時点では未確定。

-   接続先ホスト名/ポート（中継サーバのエンドポイント）
-   車両認証方式（案：デバイスごとのAPIキー、またはクライアント証明書によるmTLS）
-   GGA送信形式（既存のNMEA-GGAフォーマットを踏襲する想定）
-   接続断時の車両側リトライ仕様（バックオフ方針等）

------------------------------------------------------------------------

## データ収集サービスとの関係

計測データ（RTK
FIX位置＋センサーデータ）は中継サーバを経由せず、別途「データ収集サービス」へ送信される想定。現在データフォーマットはPOC中であり、後日連携される。中継サーバはNTRIP中継に専念し、計測データパイプラインとは責務・スケーリング特性が異なるため疎結合に保つ。

------------------------------------------------------------------------

## 未決事項

-   RTKプロバイダー未選定。選定基準の整理が必要（対応NTRIPバージョン、認証方式、API有無、料金体系、SLA、対応エリア等）
-   車両側認証方式の確定（APIキー vs クライアント証明書）
-   遅延要件の具体的な数値目標
-   想定車両台数・同時接続数
-   利用ログの用途（監視のみか、将来課金按分にも使うか）による保存先・保持期間の方針

------------------------------------------------------------------------

## 次のステップ

1.  RTKプロバイダー選定基準の整理・比較検討
2.  中継サーバのプロトコル・API仕様の詳細設計（認証方式含む）
3.  車載ソフトウェアチームへインターフェース仕様を提示
4.  AWS構成のPoC（NLB + Fargateでの疎通確認、レイテンシ計測）

# RTK中継サーバ プロトコル・認証方式 詳細設計（Draft）

[rtk_relay_server_requirements.md](./rtk_relay_server_requirements.md)
のF-1〜F-8を満たすための、中継サーバの通信プロトコルおよび認証方式を詳細化する。プロバイダーは本ドキュメント時点で未選定のため、Provider
Adapterで吸収できる形で設計する。

------------------------------------------------------------------------

## 中継サーバの二重ロール

中継サーバは、接続してくる相手によって異なる役割を演じるプロキシとして機能する。

-   **車両（NTRIP Client）から見ると** → 中継サーバは **NTRIP
    Caster**（サーバ側）
-   **RTKプロバイダーから見ると** → 中継サーバは **NTRIP
    Client**（クライアント側）

``` mermaid
flowchart LR
    subgraph V["車両側"]
        VC["NTRIP Client"]
    end
    subgraph R["中継サーバ"]
        RS["NTRIP Caster役<br/>(車両向け)"]
        RC["NTRIP Client役<br/>(プロバイダー向け)"]
        RS <-->|"車両セッションと1:1で紐付け"| RC
    end
    subgraph P["RTK Provider"]
        PC["NTRIP Caster"]
    end

    VC <-->|"NTRIP (GGA/RTCM)<br/>車両認証"| RS
    RC <-->|"NTRIP (GGA/RTCM)<br/>プロバイダー認証"| PC
```

中継サーバはRTCM/GGAの中身を解釈・加工せず、**バイトストリームをそのまま透過**させることを基本方針とする。中身を解釈すると実装・保守コストとレイテンシが増える上、RTCMのバージョン差異等をプロバイダー選定前に気にする必要がなくなる。

------------------------------------------------------------------------

## 通信シーケンス（詳細）

``` mermaid
sequenceDiagram
    participant VC as NTRIP Client(車両)
    participant RS as 中継サーバ
    participant SEC as Secrets Manager
    participant DB as 車両認証DB
    participant PC as RTK Provider

    VC->>RS: TLS接続 + NTRIP GET<br/>Authorization: Basic(車両APIキー)
    RS->>DB: 車両APIキー検証
    DB-->>RS: 有効 / 無効
    alt 無効
        RS-->>VC: 401 Unauthorized
    else 有効
        RS->>SEC: プロバイダー認証情報取得
        SEC-->>RS: Mountpoint/ID/PW
        RS->>PC: TLS接続 + NTRIP GET<br/>Authorization: Basic(provider credentials)
        PC-->>RS: ICY 200 OK
        RS-->>VC: ICY 200 OK
        loop 測位中
            VC->>RS: GGA
            RS->>PC: GGA (透過転送)
            PC-->>RS: RTCM
            RS-->>VC: RTCM (透過転送)
        end
    end
```

------------------------------------------------------------------------

## 認証方式

### 車両側認証（中継サーバへの接続）

| 方式 | 概要 | メリット | デメリット |
|---|---|---|---|
| **APIキー方式（案A）** | 車両ごとに発行したAPIキーをNTRIP標準のBasic認証枠に格納 | NTRIP標準に乗るため実装コストが低い、既存NTRIP Clientソフトのままでも移行しやすい | 漏洩リスク（キー流出時はローテーションで対応） |
| **mTLS方式（案B）** | 車両ごとにクライアント証明書を発行し、TLSハンドシェイクで認証 | なりすまし耐性が高い | 証明書のプロビジョニング・失効管理(CRL/OCSP)の運用負荷 |

**推奨：初期フェーズはAPIキー方式（案A）を採用する。**
理由は、既存のNTRIP
Client実装（車載側）がBasic認証に標準対応しており、車載チームへの追加要求が少ないため。台数増加やセキュリティ要件の厳格化に応じて、mTLSまたはAPIキー+デバイス証明書の併用へ移行する。

-   車両IDとAPIキーのペアを中継サーバ側のDB（DynamoDB想定）で管理
-   キーはハッシュ化して保存し、レインボーテーブル攻撃を防ぐ
-   キーのローテーション・失効を運用フローとして用意する（車両紛失・盗難時に即時失効できること）

### プロバイダー側認証（中継サーバからプロバイダーへの接続）

-   **`provider_id`の単位に注意**：多くのRTKプロバイダーは「1アカウント=同時接続1台まで」という契約になっている（[rtk_provider_comparison.md](./rtk_provider_comparison.md)の「車両台数が多い場合のコスト構造」参照。例外はKDDIのPPP-RTKメニューのみ）。したがって`provider_id`は「プロバイダー会社名」（`docomo`）ではなく「**個別の契約アカウント**」（`docomo-vehicle-001`）を指す単位で運用する。台数課金型プロバイダー以外では、車両1台につきプロバイダー契約1件・Secrets Managerシークレット1件が対応することになる
-   **車両 ⇔ プロバイダーのマッピング**：DynamoDBの車両認証テーブル（`vehicle_credentials`）の各アイテムに`provider_id`属性を持たせる。新規テーブルは不要（実装済み、`internal/auth`）
-   **プロバイダー接続情報**：プロバイダーIDごとに1つのSecrets
    Managerシークレット（`rtk-relay/providers/<provider_id>`）として格納する。中継サーバのIAMロールは、このプレフィックス配下への`GetSecretValue`のみをワイルドカードで許可されている。**新しいプロバイダーの追加はシークレットを1個作るだけで完結し、Terraformの変更もコードの変更も不要**（実装済み、`internal/providerconfig.SecretsManagerResolver`）
-   接続確立のたびに、認証成功した車両の`provider_id`をもとにこの仕組みで都度プロバイダー設定を解決する（起動時に1回だけ読み込む方式ではない）ため、プロバイダー追加・切替に再デプロイが不要

#### プロバイダー設定スキーマ

各社RTKプロバイダーはID/Passwordだけでなく、[rtk_provider_comparison.md](./rtk_provider_comparison.md)の調査で分かった通り、NTRIPバージョンや暗号化ポートの有無など接続時の付随パラメータが異なる。これを吸収するため、プロバイダー設定（Secrets
Managerの値）は以下のJSONスキーマとする（`internal/providerconfig.Provider`）。

``` json
{
  "host": "string (必須)",
  "port": "string (必須)",
  "mountpoint": "string (必須)",
  "username": "string (必須)",
  "password": "string (必須)",
  "ntrip_version": "\"1\" | \"2\" (省略時 \"2\")",
  "tls": "bool (省略時 false。docomoの2102ポートのようなTLS提供プロバイダー向け)",
  "gga_interval_seconds": "number (省略可。プロバイダー推奨のGGA送信間隔、参考値)",
  "extra_headers": "object (省略可。上記で表現しきれない任意のNTRIPリクエストヘッダー。例: アカウント/契約IDをヘッダーで要求するプロバイダー向け)"
}
```

`host`/`port`/`mountpoint`/`username`/`password`は調査した全社が共通して必要とする最小集合。それ以外はプロバイダーごとに有無が分かれるため、`extra_headers`を汎用の逃げ道として用意し、未知のプロバイダー要件にもコード変更なしで対応できるようにしてある。

------------------------------------------------------------------------

## セッション・接続管理

-   車両1台につき、中継サーバ~プロバイダー間は**1本の永続TCPコネクション**を1:1で対応させる（車両セッション確立時にプロバイダー側セッションも確立）
-   車両側の瞬断（数秒〜数十秒）に対しては、プロバイダー側セッションを即座に切らず一定時間（例：30秒、要検討）保持し、再接続時の認証コストを削減する
-   プロバイダー側セッションが切断された場合は自動再接続を試みる（既存要件F-6）。再接続中は車両へRTCM欠落を許容するか、明示的なステータス通知を行うかは要検討
-   中継サーバのプロセス（ECS/Fargateタスク）単位でのセッション保持となるため、タスク再起動・スケールイン時のグレースフルシャットダウン（既存セッションのドレイン）を考慮する

------------------------------------------------------------------------

## GGA送信間隔とVRS対応

-   VRS方式のプロバイダーでは、車両からのGGAに応じてプロバイダー側が仮想基準局を動的に生成・更新するため、**GGAの定期送信（一般的に1〜10秒間隔、プロバイダー仕様に依存）を継続することが前提**
-   中継サーバはGGAを解釈せず透過転送するだけでよいが、**GGA送信の途絶を検知するタイムアウト監視**は中継サーバ側で持つ（プロバイダーがセッションをタイムアウトさせる前に、車両側の異常として検知・アラート可能にするため）
-   直近のGGAをキャッシュしておき、一時的な途絶時に最終位置を保持する設計は、プロバイダー選定後に必要性を判断する（プロバイダー側のタイムアウト仕様次第）

------------------------------------------------------------------------

## データ形式・透過方針

-   **GGA**：NMEA-0183テキスト形式のまま中継（改変しない）
-   **RTCM**：RTCM 3.xバイナリストリームのまま中継（メッセージ種別の解釈はしない）
-   中継サーバはNTRIPのHTTPヘッダ部分（認証・Mountpoint選択等）のみをハンドリングし、ペイロード部分は完全にバイパスする

------------------------------------------------------------------------

## エラーハンドリング方針

| ケース | 中継サーバの挙動 |
|---|---|
| 車両認証失敗 | 401を車両へ返却、DBに失敗ログ記録 |
| プロバイダー認証失敗・Mountpoint不正 | 502相当のエラーを車両へ返却、運用アラート発報（プロバイダー側の設定ミスの可能性があるため） |
| プロバイダー側の輻輳・レート制限 | 指数バックオフで再接続、上限回数超過でアラート |
| プロバイダー側セッションタイムアウト | 自動再接続を試行、車両側へは可能な範囲で透過（再接続中はRTCM欠落） |

------------------------------------------------------------------------

## ログ・監査方針

-   記録する情報：接続開始/終了時刻、車両ID、送受信バイト数、切断理由
-   **GGA/RTCMの通信内容そのものは原則ログしない**（ストレージコスト・プライバシー配慮）
-   障害調査用に、車両ID指定で一時的にフル通信ログを取得できる「デバッグモード」を用意する（運用時の切り分けに有用）
-   ログ保存先はCloudWatch
    Logsを基本とし、長期保持・分析用途がある場合はKinesis Data
    Firehose経由でS3へアーカイブする

------------------------------------------------------------------------

## 未決事項（本ドキュメント特有）

-   NTRIP v1/v2どちらをサポートするか（プロバイダー選定後に確定。v2の方がchunked
    transfer等モダンだが、プロバイダーの対応状況次第）
-   車両側認証をAPIキーからmTLSへ移行するタイミング・トリガー
-   プロバイダーの契約単位（1台=1アカウントか等）が中継サーバのマッピングテーブル設計に与える影響
-   プロバイダー側セッションの瞬断許容時間の具体値
-   GGAタイムアウト監視のしきい値

------------------------------------------------------------------------

## 次のステップ

1.  プロバイダー選定後、対応NTRIPバージョン・契約単位を確認し、本ドキュメントの未決事項を確定する
2.  Provider
    Adapterのインターフェース定義（Go/Rust/Node等、実装言語の選定を含む）
3.  車両側APIキー発行・失効フローの運用設計
4.  PoC実装（1車両相当のトラフィックでの疎通確認、レイテンシ計測）
